# The baseline pipeline itself (data loading, FM model, BPR training, evaluation,
# agent orchestration/EDA/RAG) is numpy + Python stdlib only.
numpy>=1.24

# Required for agent/orchestrator.py runs: torch/torchfm unlock the model-zoo
# architectures in workspace/models/*.py (DeepFM/DCN/AutoInt/xDeepFM) as a
# proposable model_architecture lever. orchestrator.py's preflight_check() now
# self-installs these into its own interpreter if missing and hard-fails the run
# if that install doesn't succeed -- there is no numpy-only fallback anymore, so
# a plain `pip install -r requirements.txt` should include them for any agent run.
torch
torchfm
